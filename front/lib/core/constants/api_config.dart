class ApiConfig {
  // 👇 SOLO CAMBIÁ ESTA LÍNEA cuando la URL cambie
  static const String baseUrl = 'https://galen-unfossilised-nonrespectably.ngrok-free.dev/api';
  
  // Opcional: podés tener un flag para cambiar fácilmente entre entornos
  // static const bool isProduction = false;
  // static String get baseUrl => isProduction ? prodUrl : devUrl;
}